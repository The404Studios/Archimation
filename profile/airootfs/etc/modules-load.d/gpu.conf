# Load GPU kernel modules at boot for KMS/display.
# Do NOT force-load nouveau here — it can hang on some NVIDIA GPUs
# (GT218/NVS 3100M). Let udev auto-load it only for supported hardware.
# i915 covers Intel, amdgpu covers AMD. NVIDIA handled by udev.
i915
amdgpu
