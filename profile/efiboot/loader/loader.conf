timeout 2
default ai-arch-linux.conf
editor no
# Disable console verbose log output during boot selection for cleaner UX
console-mode keep
# Auto-select the first entry after timeout on one-shot boots (install media)
auto-entries 1
auto-firmware 1
