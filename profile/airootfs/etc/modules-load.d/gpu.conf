# Load GPU kernel modules at boot so Xorg/LightDM can start.
# nouveau covers ThinkPads and other NVIDIA hardware without nvidia-open.
# The nvidia stack is preferred when available (initramfs or DKMS).
nouveau
amdgpu
