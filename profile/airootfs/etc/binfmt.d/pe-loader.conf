# binfmt_misc registration for Windows PE executables
# Allows .exe files to be executed directly via the PE loader
# Format: :name:type:offset:magic:mask:interpreter:flags
:PE:M::MZ::/usr/bin/peloader:OCF
